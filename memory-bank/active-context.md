# Active Context

## Current Focus

- **AuK Base BF16 10초 품질 실험(2026-09-21, 커밋 전)**: 3.062GB Base BF16 확산 모델 설치 완료. Qwen은 W4A8 유지(텍스트+원본 오디오 conditioning은 이미 남성화를 성공시켜 우선 생성 모델 정밀도만 분리 비교). 첫 10초·seed 424242·guidance 0.7·steps 32·sway -1·Prompt Enhance 끔으로 추론 성공, 결과 `test/auk-base-bf16/01-base-bf16-guidance-0_7.wav`. 평가 전 기본 설정은 Base W4A8로 복원. 다음 단계는 사용자 청취 후 BF16에서 steps 비교 여부 결정.
- **AuK Base 저 guidance 실험(2026-09-21, 커밋 전)**: 원본 여성 보컬이 남성화됐지만 가창력이 크게 무너졌다는 사용자 평가에 따라 첫 10초만 동일 seed 424242, steps 32, sway -1, Prompt Enhance 끔으로 guidance 0/0.2/0.5 및 0.7/1.0/1.5를 비교 생성. 결과는 `test/auk-base-low-guidance/`. Prompt Enhance는 느슨한 요청을 TTS로 오분류했고, 정확한 요청에서는 기존 직접 템플릿과 동일해 SongYUE2 제품 경로에는 연결하지 않기로 함.
- **AuK Base 기본값 복구(2026-09-21, 커밋 전)**: 직전 실제 변환 두 번이 Flash였음을 확인. Tools 메뉴는 이미 Base였지만 음색 변조 UI 초기값, SongYUE2의 잘못된 체크포인트 fallback, AudioAuK 저장 설정 및 신규 설치 기본값이 Flash로 남아 있어 모두 Base W4A8로 통일. 실행 중인 AudioAuK에도 즉시 반영. 설치 스크립트는 Base+Flash 모두 다운로드하고 진단 필수 모델은 Base를 확인하도록 변경. SongYUE2 check/test(22), AudioAuK test(2) 통과.
- **AuK 장음원 청크 처리 + 영어 남성 음색 재실험 + AudioContext 오류 수정(2026-09-21, 커밋 전)**: 참조 없이 음색 설명으로 처리할 때 10초 청크를 8초 간격으로 잘라 2초 오버랩하고, 첫 청크는 뒤 1초·중간 청크는 앞뒤 1초·마지막 청크는 앞 1초를 잘라 연결. 전 청크에 동일 seed를 써 음색 흔들림을 줄였고 실제 115.04초 입력을 Flash 15청크로 완주. 한국어 `따듯하고 중후한 남성` 결과에서 여성 음색이 유지됐다는 사용자 평가에 따라 성별·음역·흉성·발성을 명시한 영어 prompt로 같은 입력을 재처리해 `test/auk-chunk-comparison/05-06` 파일로 보존. HMR 뒤 닫힌 `AudioContext` 재사용/중복 종료 오류도 수정. `npm run check` + `npm test`(22개) 통과.
- **Tools 페이지 재설계(2026-09-20, 커밋 전)**: "오디오 도구 (실험적)" → "Tools (실험적)" 전체 명칭 변경 + 경고 문구 제거. 좌/우 40:60 분할, 탭-입력 간격 확대. 좌측 순서 "오디오 선택 → 모델 선택 → Function 선택 → 가변 필드 → 실행"으로 재구성 — TTS 카테고리는 sub-tabs 대신 가사/대사 편집과 동일한 `at-function` 체크 칩(selectTool 재사용). 결과 패널은 음원 비교 패턴으로 **원본/처리본 2행 동시 표시**(파일 선택 즉시 원본 행 렌더, 처리본은 대기 상태 포함), 각 행이 자기 실제 duration으로 X축을 그림(10초 고정 해소). `npm run check` + `npm test`(22) 통과. 커밋 대기.
- **AuK 가사 오염("아. 아. 아.") 해결 — 저장 가사 사용(2026-09-20, 커밋 전)**: 답변 확인됨 — ① "아.아.아."는 Whisper STT 할루시네이션(모델 문제); ② AuK는 YUE2 재생성이 아니라 AudioAuK zero-shot TTS voice-clone — 레퍼런스 있으면 소스 보컬을 전사해 그 가사를 레퍼런스 목소리로 낭독(멜로디/리듬 미유지, 이는 제품 제약), 텍스트만 있으면 change-timbre; ③ 앱에서 만든 저장곡은 오디오 옆 `{제목}.json`에 가사 있음 → 신규 `GET /api/library/meta`로 조회해 쓰고, 외부 오디오(가사 없음)면 기존 Whisper 전사로 다시 만들어 표시. 구현 완료(`applyAukTimbreCore`/`auk/apply`에 `lyrics`, `auk.mjs` reference 분기에서 lyrics 있으면 `/api/transcribe` 스킵+구간표기 제거, 프론트 `sourceLyrics` state/라벨 구분). `npm run check` + `npm test`(22개) 통과. **백엔드 재시작 필요(신규 라우트).** 커밋 대기 중.
- **음색 변조 레거시 목록 개편 + 파형 정렬(2026-09-20, 커밋 전)**: 사용자 요청대로 레거시 목록을 "원곡 > 원곡 보컬 > 원곡 악기 > 참고곡 > 참조 보컬 > 변환곡 > 변환곡 보컬" 순서로 변경 — 변환곡 악기 행 제거, 참고곡 뒤에 "참조 보컬" 행 추가(신규 `POST /api/timbre-transform/reference/separate` → `separateReferenceVocal()`: 참조 오디오를 mel_band_roformer로 즉석 분리, preview 캐시 없음. 프론트는 참조 선택 시 비동기 로드 + `referenceSepTokenRef` 토큰으로 연속 클릭 시 마지막만 반영). 동시에 원곡/원곡 보컬 왼쪽 edge·파형 길이 정렬 — 원인은 `.stem-row-sub`의 `margin-left:24px`로 행 전체가 밀려 파형 좌측 edge/폭이 어긋났던 것, `timbre-legacy-list .stem-row-sub{margin-left:0}`로 해제. `npm run check` + `npm test`(22개) 통과. 커밋 대기 중(오디오 도구 재설계와 함께 분리 커밋 여부 확인).
- **오디오 도구(실험적) 페이지 재설계 구현(2026-09-20, 커밋 전)**: 상단 카테고리 탭(유지)+하위 도구 탭, 좌(입력 30%)/우(결과 70%) 2열, 결과 영역은 "음원 비교" 모듈(waveform+spectrogram+seek bar+transport+취소/저장) 재사용. TTS 카테고리를 `TTS (T2S)`(오디오 없음, 출력 길이 필수)와 `TTS 생성 (Ref-T2S)`(참조 오디오, 기본 0=참조 길이) 2탭 분리. 가사/대사 편집은 Function 체크박스(교체/삽입앞/삽입뒤/삭제) 단일 선택 + 오디오 선택 시 자동 전사(신규 `POST /api/audio-tools/transcribe`) 텍스트 상자. 신규 `app/app/audio-tools.css`, `normalizeInputAudio` 헬퍼(auk/transcribe 공유, 모노 44.1kHz), `readFileAsDataUrl` 모듈 스코프 승격. `npm run check` + `npm test` 통과. 아직 커밋 안 함.
- **음색 변조(실험적) 파형 미표시 버그 수정(2026-09-20)**: Seed-VC/Vevo에서 원본/참조 선택 시 오른쪽 파형이 안 보이고 아이콘·라벨·파형이 세로로 쌓이는 문제를 수정 — 원인은 `TimbreTransformDialog`가 재사용하는 `audio-compare-dialog` 클래스의 `.stem-row{flex-direction:column}`(+column flex에서 `.pp-waveform`의 flex-basis:0 높이 붕괴). 레거시 목록에 `timbre-legacy-list` 클래스를 추가하고 해당 행만 가로 단일 행(아이콘 왼쪽+파형)으로 복원. AuK/DDSP 차트 뷰는 무변경.
- 코드 감사(2026-09-20) 발견사항: DDSP 취소가 실제 중단이 아님, `/timbre-transform/prepare`에 generating 락 부재(미수정 — 다음 세션 우선순위), `withEstimatedProgress` 100% 오표시, Vevo2 미반영 문서 drift(README·model-download-status.json) 등 — `memory-bank/knowledge/`로 flush 완료.
- **`todo.md` 밀린 항목 정리(2026-09-20)**: "커버→노래 만들기 완주 검증"(API 레벨로 재현 시도했으나 에러 없이 정상 완주 — 이전에 원인 불명 에러로 막혔던 기록은 이후 다른 세션들의 GGUF/ABC 수정으로 이미 해결된 것으로 보임), ABC "파일에서 가져오기" 브라우저 종단 검증(raw 텍스트+JSON 래핑 둘 다 확인), EQ/전체 설정 프리셋 내보내기·가져오기 신규 구현(기존 라우트 재사용, `showSaveFilePicker`/`<a download>` 양쪽 실기 검증) — 전부 완료. 상세는 `todo.md`.
- **DDSP-SVC 품질 개선 실험 4종 완료, 종합 결론(2026-09-20)**: pitch extractor 6종+vocoder는 기존 100k스텝 체크포인트로 추론만 재실행(저비용)해 비교 — 둘 다 원곡과의 거리가 0.117~0.125로 좁아 원인 아님. Feature encoder(ContentVec↔HubertSoft)만 재학습이 필요해 HubertSoft를 동일 데이터셋·동일 하이퍼파라미터로 100k스텝까지 실제 재학습(~3시간)해 비교 — **세 체크포인트(40k/70k/100k) 전부 ContentVec이 HubertSoft보다 원곡에 더 가까움**(가설과 반대 방향, 바꿀 이득 없음). 데이터셋 확대는 같은 목소리의 추가 레퍼런스가 없어 실행 불가. **결론: "음색이 좁다"는 소견의 원인은 이 4개 파라미터가 아니라 데이터셋 크기(81클립/12분, 단일 화자) 자체일 가능성이 높음** — 검증하려면 사용자가 같은 목소리의 새 레퍼런스 클립을 구해와야 함. 상세: `test/vocal-timbre-engine-comparison/README.md`.
- **"음색 변조"/"오디오 도구" 실기 검증 완주 + DDSP-SVC 체크포인트 버그 발견·수정(2026-09-19)**: 실제 GPU로 `targetStep` 작은 학습을 돌려보니 kill-at-target 메커니즘은 정상이었지만, `configs/reflow.yaml`의 `interval_val: 2000`(체크포인트 저장 주기) 때문에 작은 목표 스텝에서는 체크포인트가 단 한 번도 안 만들어져 실패 — `backend/ddsp-svc.mjs`에서 `interval_val`을 `targetStep`에 비례해 패치하도록 수정, 재검증 성공. **교훈**: [[interval-based-checkpoint-cadence-mocks]] — 주기적으로만 발생하는 부작용을 mock으로 시뮬레이션할 때는 실제 주기 상수를 읽어서 반영해야 "항상 자주 일어난다"는 낙관적 가정이 드문 이벤트 의존 버그를 가려버리는 일을 피할 수 있다.

## Pending / Not Yet Done — 큰 설계 결정·새 엔진 설치가 필요해 사용자 확인 후 진행

- **STEM 분리 모델 선택 UI**: 지금은 모드당 모델이 1개뿐이라(`STEM_MODES`) 진짜 "선택"의 의미가 없음 — 6스템 Demucs(`htdemucs_6s`) 같은 새 옵션을 실제로 추가하려면 별도 독립 Python Demucs 설치가 필요.
- **audio.cpp 미사용 모델군**: Stable Audio(인페인팅/컨티뉴에이션), ACE-Step GGUF 네이티브 경로, RVC — 전부 "발견만 된 후보", 다운로드/실측 후 채택 여부 판단 필요.
- **"DDSP Func" 팝업**(실시간 변환/음색 믹싱/다중 화자): DDSP-SVC를 실제 SVC 엔진으로 채택 확정 시 같이 설계하기로 보류.
- **프론트엔드 자동화 테스트 없음**: 지금까지 전부 수동 브라우저 검증(Playwright)만 — 회귀 테스트 스위트 자체가 큰 신규 이니셔티브라 별도 논의 필요.
- **File System Access API 비-Chromium 폴백**: 이 환경에 비-Chromium 브라우저가 없어 검증 불가, 계속 보류.
- 그 외 세부 항목은 `todo.md` 참고(단일 진실 공급원으로 유지).
