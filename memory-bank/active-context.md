# Active Context

## Current Focus

- **완성곡 메뉴에 "커버" 추가(2026-09-13)**: 리믹스 아래 신설. SheetSage2로 그 곡의 오디오를 전사해 ABC를 뽑아 작곡 화면에 채움. 활성화 조건은 **그 곡을 만든 모델이 아니라 현재 선택된 모델**이 원본인지 여부(사용자가 직접 지적해 수정) — 전사 자체는 오디오 출처와 무관하지만 ABC를 실제로 쓰는 건 지금 선택된 모델이기 때문. 이미 가사/스타일이 있으면 유지/교체 확인 대화상자, 어느 쪽이든 modelId는 안 건드림. Suno류와 달리 실제 보컬 음색/톤은 못 가져옴(YuE2에 참조오디오 화자 임베딩 입력 자체가 없음, `progress.md`에 한계로 기록). 브라우저에서 ABC 생성+설정 채우기까지 검증(실제 "노래 만들기" 생성은 별도 이슈로 범위 밖).
- **ABC 기반 생성(심볼릭 작곡/커버)은 GGUF에서 전면 차단(2026-09-13)**: `runAudioCpp()`가 애초에 `--abc-file`을 지원 안 해서 조용히 무시되던 함정을 발견 → 프론트 버튼 비활성화 + `/api/generate` 백엔드 가드 추가.
- **재생 중인 곡 카드 강조 + 전역 재생바 선형 파형(2026-09-13)**: `now-playing` 클래스로 초록 테두리+글로우(내 작업/홈/라이브러리/좋아요/재생목록 전부). 하단 전역 재생바에는 후처리 다이얼로그의 원형 비주얼라이저를 그대로 설정 재사용하되 좌표만 직선으로 바꾼 버전 추가 — 전역 `<audio>`엔 Web Audio 그래프가 없었어서 `AnalyserNode`를 첫 재생 시 한 번만 생성(`createMediaElementSource`는 엘리먼트당 평생 1회 제약).
- **SheetSage2 제로샷 커버 — 종단 검증 완료(2026-09-12)**: 별도 venv 구축 후 실제 오디오로 종단 성공. 버그 2개 수정: `config.json`의 `weights_format` 오기(`adapter`→`merged`, models/는 gitignore라 재다운로드 시 다시 고쳐야 함), `backend/server.mjs`의 출력 폴더 사전생성이 `transcribe.py`의 `fresh_directory()` 안전장치와 충돌하던 것.
- **"악기만"은 GGUF에서 아예 선택 불가**: 모델 전환/프로젝트 로드/localStorage 복원 3개 경로 모두에서 `instrumental`이 비-Python 모델에 stuck되지 않도록 정리됨.

## Pending / Not Yet Done

- "커버"로 채운 내용으로 실제 "노래 만들기"까지 완주하는 종단 검증 — 현재 실제 생성이 원인 불명 에러로 중간에 끊기는 별도 문제가 있어 막혀 있음.
- `mute-voice` 전환이 실제로 화성이 유지된 무보컬 오디오를 만드는지 실기 검증 필요.
- INT8 ConvRot 어댑터 연동, ABC 파일 가져오기, 비-Chromium 폴백은 여전히 실제 환경 종단 검증 전.
- 프론트엔드 자동화 테스트 없음(수동 브라우저 검증만).
- EQ/전체 설정 프리셋 내보내기·가져오기는 여전히 미구현(우선순위 낮음).
